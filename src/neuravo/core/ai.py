"""Reserved module.

Your v1.0 tree lists ``core/ai.py`` alongside ``client.py``/``config.py``/
``exceptions.py`` but its intended contents weren't specified. Left as an
empty placeholder rather than guessing at behavior — confirm what this
should hold (e.g. a top-level ``AI``/session facade distinct from
``Client``?) before anything is added here.
"""
